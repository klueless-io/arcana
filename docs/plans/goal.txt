Ship v0.2.1 of Arcana with three new kernel methods (query.getNeighbors, query.listContradictions, query.listInsights) as thin facades over the existing StructuredStore provider methods, plus two parallel read-only audits, but do NOT execute npm publish. Done when ALL of: (1) the three methods in /Users/davidcruwys/dev/kybernesis/arcana/packages/arcana-core/src/access/query/index.ts return QueryResult-wrapped results instead of throwing NotImplementedError; (2) new tests in packages/arcana-core/src/access/query/index.test.ts cover positive + filter cases for each; the still-stubbed block no longer asserts on these three; (3) `bun run build` exits 0 and `bun run test` exits 0 with ≥ 238 tests; (4) all 5 package.json files under packages/*/ show "version": "0.2.1"; (5) CHANGELOG.md (create if missing) has dated sections for v0.2.0 and v0.2.1; (6) .mochaccino/data/06-kernel-methods.json shows the three methods with "status": "implemented" and "driven_by" populated; the summary block reflects new counts (20 implemented / 8 stubbed / 0 missing) and new test count; (7) .mochaccino/views/kernel-methods.html reflects the same — three pills flipped to ✓ done, headline counters updated; (8) .mochaccino/views/index.html stats updated to new test + impl counts; (9) a fresh ARCANA → KBOT entry dated 2026-05-20 is appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md announcing v0.2.1 and noting the arcana.providers.structured.* escape hatch for any other unimplemented query facade; (10) a single feat commit lands on main with the implementation + tests + data + comms; a separate chore commit covers the version bump; (11) git tag v0.2.1 created locally; (12) `git push origin main && git push origin v0.2.1` executed; (13) Tier 2 audits: spawn two Explore agents in parallel — one to audit the buildFtsQuery function in packages/arcana-provider-libsql/src/libsql-structured-store.ts for FTS5 syntax-injection edge cases, one to cross-check the hybridSearch RRF impl in packages/arcana-core/src/retrieve/index.ts against KyberBot's parallel impl at /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/brain/hybrid-search.ts for behavioural deltas; if either surfaces material findings, fold them into the v0.2.1 commit BEFORE tagging and document in the Findings appendix of docs/plans/2026-05-20-tier1-tier2-facades-and-audits.md. Check: `npm view @kybernesis/arcana-contracts@0.2.1 version` returns 404 (publish NOT executed), `git ls-remote --tags origin v0.2.1` returns the tag, `bun run test` exits 0. Out of scope and MUST NOT be touched: sleep pipeline methods, Brain-domain stubs (readBlock/getBlockHistory/updateBlock), no-demand stubs (deleteMemory/stats), LLMProvider impl, arcana-provider-postgres, any file in /Users/davidcruwys/dev/kybernesis/kyberbot or /Users/davidcruwys/dev/kybernesis/kybernesis-brain, npm publish itself. Plan source of truth: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-20-tier1-tier2-facades-and-audits.md. Stop after 40 turns if not converged.
