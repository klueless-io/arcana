Ship v1.0.0: deepen Fact schema + fact-FTS5 + rich-bundle factRetrieval — port from KyberBot fact-store.ts per ADR 011 + ADR 013. NO npm publish. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-22-v2-fact-retrieval.md. KB sources (READ FIRST): /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/brain/fact-store.ts + fact-retrieval.ts + the extract-facts impl. Done when ALL of: (1) FactSchema widened — entities: z.array(z.string().min(1)) replaces single entity; sourceMemoryId? sourcePath? sourceConversationId? optional; category: FactCategorySchema required default 'general'; FactCategorySchema = z.enum(['biographical','preference','event','relationship','temporal','opinion','plan','general']) verbatim from KB fact-store.ts:38-46; widenLegacyFact(old) migration helper exported; round-trip tests updated; (2) StructuredStore.searchFactsFulltext(opts: FulltextSearchOpts & {category?: FactCategory}): Promise<FulltextMatch[]> declared in arcana-contracts; matchedFields populated with 'content'|'entities'; (3) arcana-provider-libsql connect() migration adds columns entities_json/source_memory_id/source_path/source_conversation_id/category; creates facts_fts USING fts5(content, entities) + INSERT/UPDATE/DELETE triggers; indices idx_facts_category/idx_facts_source_memory_id/idx_facts_source_conv; mirror KB fact-store.ts:102-225; 10KB input cap on FTS query; searchFactsFulltext via FTS5 MATCH, score 0..1 normalised; (4) arcana-core ingest.extractFacts populates entities[], category, sourceMemoryId=memory.id, sourcePath/sourceConversationId from memory metadata; LLM prompt ported from KB extract-facts impl; rejects empty entities[]; defaults category 'general'; (5) arcana-core retrieve.factRetrieval gains Layer 0 (direct fact-FTS via searchFactsFulltext, scored 0.5+matchRatio*0.5) before existing 4 layers; why='fact-retrieval/direct_facts'; priority bridge>direct_facts>direct>entity_expansion>graph_expansion; return shape FactRetrievalResult { facts: ScoredFact[]; supportingMemories: HybridSearchResult[]; assembledContext: string; tokenEstimate: number; stats: { perLayerCounts; totalCandidates; deduplicatedCount } }; field names verbatim from KB fact-retrieval.ts; tokenEstimate = Math.ceil(assembledContext.length / 4); (6) all 6 packages bumped 0.5.0 → 1.0.0; (7) bun run build exits 0; (8) bun run test exits 0 with ≥ 295 tests; (9) CHANGELOG.md v1.0.0 references ADR 013 + ADR 011, lists 5 breaking changes, names KB sources; (10) Mochaccino refreshed — 04-contracts-surface, 06-kernel-methods (test count + v1.0.0 summary); views index.html / kernel-methods.html / package-graph.html bumped to v1.0.0; (11) ARCANA → KBOT entry dated 2026-05-22 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md — v1.0.0 BREAKING, schema-depth gap closed, default action: KB brings fact fixtures + invokes runParityHarness, 100% parity on memory-id set + fact-id set; bounce-back rules listed; (12) two commits on main — feat then chore (6 version bumps); (13) git tag v1.0.0 created and pushed; (14) git push origin main + git push origin v1.0.0; (15) Findings appendix populated with concrete resolutions for: KB extraction prompt shape, legacy-fact widening, FTS5 trigger ordering, rich-bundle field names, token-estimate divisor — each citing KB file:line + Arcana code location. Check: npm view @kybernesis/arcana-contracts@1.0.0 version returns 404, git ls-remote --tags origin v1.0.0 returns tag, bun run test exits 0. Out of scope: sleep pipeline (next sprint), v2 sleep extras, arcana-provider-llm-http, postgres/convex searchFactsFulltext impls, KB swap, npm publish, KyberBot/Brain repo changes. Stop after 50 turns if not converged.
