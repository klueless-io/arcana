Ship v0.4.0 of Arcana: rebase retrieve.hybridSearch onto KyberBot's empirical implementation per a new "port first, improve later" policy in ADR 011. Do NOT execute npm publish. HybridSearchResult shape unchanged; only internal channel topology + scoring change. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-21-hybrid-search-rebase.md. Done when ALL of: (1) docs/decisions/011-port-first-improve-later.md exists capturing the principle (KyberBot=harness, Arcana=brain, port faithfully before improving, speculative redesigns go to v2); (2a) MemorySchema gains required createdAt: string (ISO 8601); ingest.storeMemory defaults to new Date().toISOString(); arcana-provider-libsql adds `created_at TEXT NOT NULL` to memories DDL with idempotent ALTER TABLE migration on connect; rowToMemory/memoryToRow mapped both ways; existing test fixtures gain createdAt; (2b) hybridSearch no longer calls structured.getNeighbors for graph expansion — replaced with 4 channels: semantic + keyword(searchFulltext) + temporal(created_at DESC) + entity-name-filter; RRF k=60, topK*3, reranker, per-channel failure isolation preserved; (3) HybridSearchResult.matchType vocab restored to 'semantic'|'keyword'|'both' (v0.2.0 values 'graph' and 'multi' removed); (4) HybridSearchInput.graphHops accepted by the type but ignored at runtime (deprecated, documented); (5) HybridSearchResult.graphScore emitted as 0 (deprecated); (6) packages/arcana-core/src/retrieve/index.test.ts updated — graph-expansion test replaced with temporal-channel test, entity-name-filter test added, multi-channel test expects 'both' not 'multi', smoke test using runParityHarness from @kybernesis/arcana-testkit/parity asserting passes:true against same-impl baseline; existing RRF/reranker/failure-isolation/topK/empty tests still pass; (7) `bun run build` exits 0; (8) `bun run test` exits 0 with ≥ 256 tests; (9) all 5 packages at 0.4.0; (10) CHANGELOG.md has dated v0.4.0 section explaining rebase, channel topology change, deprecated fields, referencing ADR 011; (11) .mochaccino/data/06-kernel-methods.json updates hybridSearch entry + test count; 03-publish-pipeline.json adds v0.4.0 lane (status not_started — publish handed back); (12) .mochaccino/views/kernel-methods.html, index.html bumped to v0.4.0 + new test count; publish-pipeline.html package chips + new lane added (tag-row green after tagging, publish rows not_started); (13) ARCANA → KBOT entry dated 2026-05-21 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md announcing v0.4.0, explaining rebase rationale, naming new channel topology, updating parity expectation to 100% (not 60–75%), naming ADR 011 as new governing principle; (14) two commits on main — feat (rebase + ADR + tests + docs + mochaccino + comms) then chore (version bump); (15) git tag v0.4.0 created locally and pushed; (16) `git push origin main` and `git push origin v0.4.0` executed; (17) judgment calls during port (memory-id dedup vs KB's MAX_SEGMENTS_PER_PARENT, temporal ordering against memories schema, entity-name-filter against entities+edges) documented in Findings appendix of the plan. Check: `npm view @kybernesis/arcana-core@0.4.0 version` returns 404 (publish NOT executed), `git ls-remote --tags origin v0.4.0` returns the tag, `bun run test` exits 0. Out of scope and MUST NOT be touched: sleep pipeline rebase, factRetrieval rebase, getEntityProfile generalisation, resurrecting graph-BFS in hybridSearch (deferred to v2 — but leave structured.getNeighbors intact since factRetrieval uses it), HybridSearchResult shape changes, StructuredStore.searchFulltext contract changes, npm publish itself, sleep stubs, deleteMemory, stats, updateBlock, LLMProvider impl, arcana-provider-postgres, any file in /Users/davidcruwys/dev/kybernesis/kyberbot or /Users/davidcruwys/dev/kybernesis/kybernesis-brain. Stop after 60 turns if not converged.
