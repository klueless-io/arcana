Ship v0.3.0 of Arcana with a public parity-harness utility in @kybernesis/arcana-testkit (per ADR 009 §"Future evolution"), but do NOT execute npm publish. Done when ALL of: (1) /Users/davidcruwys/dev/kybernesis/arcana/packages/arcana-testkit/src/parity/index.ts exports `runParityHarness<TResult, TId>(input): Promise<ParityReport>` plus the `ParityHarnessInput` and `ParityReport` types as specified in docs/plans/2026-05-20-parity-harness.md §2; (2) packages/arcana-testkit/package.json declares "./parity" subpath export mirroring the existing "./fakes" shape; (3) packages/arcana-testkit/src/parity/index.test.ts covers — passing case (high overlap → passes:true), failing case (disjoint → passes:false), partial-overlap-at-threshold boundary, per-query missingFromCandidate / extraInCandidate diff correctness, empty corpus (meanOverlap:0, passes:false), baseline-throws (error captured with side:'baseline'), candidate-throws (error captured with side:'candidate'), custom threshold honored, custom topN honored, extractIds returning empty → 0 overlap; (4) `bun run build` exits 0; (5) `bun run test` exits 0 with ≥ 248 tests; (6) all 5 packages bumped 0.2.1 → 0.3.0 (grep '"version"' packages/*/package.json shows 0.3.0 across the board); (7) CHANGELOG.md has a dated v0.3.0 section naming the new export and referencing ADR 009; (8) packages/arcana-testkit/README.md has a Parity harness section with KyberBot hybrid-search swap as the worked example; (9) .mochaccino/data/05-testkit-compliance.json reflects the new harness (parity row or section added); (10) .mochaccino/data/06-kernel-methods.json test count bumped; (11) .mochaccino/data/03-publish-pipeline.json gains a v0.3.0 lane (status not_started — publish handed back to user); (12) .mochaccino/views/index.html stats updated (test count, tagline reflects v0.3.0 candidate); (13) .mochaccino/views/kernel-methods.html headline counters reflect new test count; (14) a fresh ARCANA → KBOT entry dated 2026-05-20 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md announcing the harness, including a usage example, the parity-audit expectation (60–75% overlap on channel-topology divergence per v0.2.1 finding), and a default action of "author parity test in KyberBot repo when convenient; bounce-back via QUESTION if you want help designing the fixture corpus"; (15) two commits on main — a feat commit with harness + tests + docs + mochaccino + comms, then a chore commit with the version bumps; (16) git tag v0.3.0 created locally and pushed; (17) `git push origin main` and `git push origin v0.3.0` executed. Check: `npm view @kybernesis/arcana-testkit@0.3.0 version` returns 404 (publish NOT executed), `git ls-remote --tags origin v0.3.0` returns the tag, `bun run test` exits 0. Out of scope and MUST NOT be touched: running an actual parity test against KyberBot's data, order-aware scoring (NDCG / Spearman), failure-triage tooling beyond perQuery payload, CLI wrapper, fixture loader utilities, multi-version parity, npm publish itself, sleep pipeline methods, Brain-domain stubs (readBlock / getBlockHistory / updateBlock), no-demand stubs (deleteMemory / stats), LLMProvider impl, arcana-provider-postgres, any file in /Users/davidcruwys/dev/kybernesis/kyberbot or /Users/davidcruwys/dev/kybernesis/kybernesis-brain. Plan source of truth: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-20-parity-harness.md. Stop after 50 turns if not converged.
