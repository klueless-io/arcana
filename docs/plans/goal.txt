Ship v1.1.0: implement the sleep pipeline in packages/arcana-core/src/maintain/ — port KyberBot's 10 sleep steps to Arcana's provider interfaces per ADR 011 (port-first) + ADR 010 resolution. Do NOT execute npm publish. Plan: /Users/davidcruwys/dev/kybernesis/arcana/docs/plans/2026-05-22-sleep-pipeline.md. KB sources (READ FIRST): /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/brain/sleep/index.ts (orchestrator), /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/brain/sleep/config.ts (SleepConfig), /Users/davidcruwys/dev/kybernesis/kyberbot/packages/cli/src/brain/sleep/steps/ (10 step files). Done when ALL of: (1) SLEEP_STEPS enum replaced with KB's 10 steps (camelCase): decayMemories|refreshTags|consolidateMemories|linkMemories|tierMemories|summarizeMemories|observeConversations|rebuildUserProfile|runReasoning|cleanEntityGraph — deferred 5 Arcana-invented steps noted in comment citing ADR 011; (2) SleepConfig interface + DEFAULT_CONFIG ported verbatim from KB config.ts into packages/arcana-core/src/maintain/config.ts; (3) packages/arcana-core/src/maintain/steps/ has 10 files each exporting run<Name>(deps: MaintainDeps, config: SleepConfig): Promise<StepResult> — adapted to use provider interfaces (StructuredStore/VectorStore/EmbeddingProvider/LLMProvider) instead of KB's direct DB access; (4) runSleepPipeline executes all 10 steps in KB order, respects input.steps filter, returns SleepRunResult{startedAt,finishedAt,stepsRun,candidatesProcessed}; (5) startSleepSchedule(intervalMs) delegates to deps.scheduler.schedule; stopSleepSchedule() cancels it; (6) orchestrator tests: steps called in order, filter respected; mechanical step smoke tests (decay/consolidate/tier/entityHygiene) with in-memory StructuredStore fake; LLM step tests (tag/summarize/observe/profile/reasoning) with vi.mock LLM stub; Scheduler mock tests; (7) bun run build exits 0; (8) bun run test exits 0 with ≥325 tests (301 baseline + ~24 new); (9) all 6 packages at v1.1.0; (10) CHANGELOG v1.1.0 section: 10-step sleep pipeline, ADR 010 resolution, ADR 011 port-first, 5 deferred steps named; (11) Mochaccino refreshed — kernel-methods (sleep pipeline 0%→100%), publish-pipeline (v1.1.0 lane not_started); (12) ARCANA→KBOT entry dated 2026-05-22 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md — v1.1.0, sleep pipeline 10 steps live, default action: KB can wire startSleepAgent to consume maintain.startSleepSchedule; (13) two commits on main (feat: sleep pipeline + chore: v1.1.0 bumps) + git tag v1.1.0 + push; (14) Findings appendix populated with concrete resolutions: KB root→Arcana provider adapter pattern, step name mapping table, checkpoint mechanism, any missing StructuredStore methods added, Scheduler contract usage. Out of scope: v2 sleep steps (collectCandidates/ingestionValidation/extractFacts-in-sleep/detectContradictions/computeSurprisal), sleep.db telemetry table, arcana-provider-llm-http, arcana-provider-postgres, npm publish, KyberBot/Brain repo files. Stop after 50 turns if not converged.
