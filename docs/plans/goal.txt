Ship v2.0.0: rename library arcana → cortex per KyberBot comms 2026-05-23 10:30 (KybernesisAI/arcana cloud product owns the name). NO npm publish, NO GitHub repo rename, NO local dir rename (David handles all three). Plan: docs/plans/2026-05-23-rename-to-cortex.md. Prerequisite (DO NOT proceed without): v1.2.1 must be live on npm AND KyberBot's harness confirmed near-1.0 meanOverlap on the 2026-05-23 09:45 + post-v1.2.1 re-run. Done when ALL of: (1) 6 package dirs renamed packages/arcana-* → packages/cortex-* via git mv; (2) each package.json name updated to @kybernesis/cortex-* + workspace deps rewritten to @kybernesis/cortex-*; (3) every import in every .ts file across all packages: @kybernesis/arcana-X → @kybernesis/cortex-X (including subpath imports like /fakes /parity); zero @kybernesis/arcana- strings remain in src/; (4) factory + types renamed: createArcana → createCortex, Arcana → Cortex, ArcanaOptions → CortexOptions, ArcanaApi → CortexApi; file packages/cortex-core/src/access/bindings/create-arcana.ts renamed to create-cortex.ts; (5) all logger debug strings 'arcana.X' → 'cortex.X' (observable in consumer logs); error messages 'arcana-core: …' → 'cortex-core: …'; (6) root + per-package tsconfig.json references updated to packages/cortex-*; (7) bun install succeeds (lock regenerates); (8) bun run build exits 0; (9) bun run test exits 0 with ≥352 tests (no test changes — rename only; any test fixtures referencing old names get updated); (10) all 6 packages at v2.0.0; (11) CHANGELOG.md v2.0.0 section explains rename + migration recipe (sed @kybernesis/arcana- → @kybernesis/cortex- + createArcana → createCortex) + cites ADR 014; (12) new ADR docs/decisions/014-library-rename-arcana-to-cortex.md authored covering why (Ian's cloud product conflict at arcana.kybernesis.ai), what (npm scope + factory + types renamed), migration path; (13) docs/decisions/README.md indexes ADR 014; (14) README.md, SPEC.md, PLAN.md, docs/SYSTEM-HEALTH.md all active references to "Arcana" project name swapped to "Cortex" (historical sprint plans, old ADRs 001-013, session checkpoints, audits — LEAVE alone, they're frozen at-time-of-writing); (15) mochaccino refresh — .mochaccino/data/02-package-graph.json + 03-publish-pipeline.json + 06-kernel-methods.json all updated (package names, v2.0.0 lane not_started, milestone, sprint note); .mochaccino/views/index.html + package-graph.html + publish-pipeline.html + kernel-methods.html — taglines, chips, package labels refreshed; (16) ARCANA→KBOT comms entry dated 2026-05-23 appended to /Users/davidcruwys/dev/kybernesis/.comms/arcana-kyberbot.md announcing v2.0.0 cortex packages staged + migration recipe for KyberBot's arcana-adoption branch; (17) two commits on main: feat (the rename + ADR 014 + comms) + chore (v2.0.0 bumps); (18) git tag v2.0.0 + git push origin main + git push origin v2.0.0; (19) Findings appendix populated in plan doc with concrete resolutions + any surprises (e.g. circular import edge cases during the rename, tsconfig path quirks, bun.lock regeneration outcome). Verify: npm view @kybernesis/cortex-contracts@2.0.0 returns 404 (not published yet); git ls-remote --tags origin v2.0.0 returns tag; bun run test exits 0; grep -r "@kybernesis/arcana-" packages/ --include="*.ts" returns zero matches. Out of scope: npm publish, GitHub repo rename klueless-io/arcana → klueless-io/cortex, local dir rename ~/dev/kybernesis/arcana → ~/dev/kybernesis/cortex (David does all three after this sprint ships), git remote set-url after GitHub rename, KyberBot dep bump on their arcana-adoption branch (their side), brain doc rename at ~/dev/ad/brains/kybernesis/arcana-spec.md (outside repo), comms file rename (separate decision, keep as-is for history continuity), historical sprint plans/ADRs/reviews/audits (frozen). Stop after 80 turns if not converged.
