/**
 * The `createArcana()` factory lands in T8 — it wires the four zone factories
 * (ingest, retrieve, maintain, access.query, access.command) into a single
 * Arcana instance with providers injected via closure.
 *
 * v0.1 placeholder — exports nothing yet. Adding code here is T8's job.
 */
export {};
